"use strict";

const sectionContainer = document.getElementById('main');
const sectionElement = document.createElement('div');
sectionElement.className = `section accueil`;
sectionElement.id = `accueil`;
sectionElement.innerHTML = `<h2>TITI Pharell</h2> <h3>Etudiant</h3>`;
sectionContainer.appendChild(sectionElement);